# ========================================
# Database Configuration (Neon PostgreSQL)
# ========================================
# Get your Neon Database URL from: https://console.neon.tech
DATABASE_URL=postgresql://user:password@hostname/database?sslmode=require

# ========================================
# Google Cloud Platform Configuration
# ========================================
# Project ID from GCP Console: https://console.cloud.google.com
GCP_PROJECT_ID=your-gcp-project-id

# ========================================
# Google Cloud Storage Configuration
# ========================================
# Bucket name for storing resumes and files
GCS_BUCKET_NAME=your-bucket-name

# Service account credentials (for Vercel, use base64 encoded JSON)
# Local development: path to key file
# Production: entire JSON as string or base64
GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json
# OR (for Vercel):
# GOOGLE_APPLICATION_CREDENTIALS={"type":"service_account","project_id":"..."}

# ========================================
# Google OAuth Configuration
# ========================================
# Create OAuth credentials: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret

# OAuth redirect URI (set in Google Console)
# Development: http://localhost:5173/api/auth/callback
# Production: https://your-domain.vercel.app/api/auth/callback
BASE_URL=http://localhost:5173

# ========================================
# Session & Security Configuration
# ========================================
# Generate a random 32+ character secret: openssl rand -base64 32
SESSION_SECRET=your-random-session-secret-here-min-32-characters-long
# Session expiry in milliseconds (86400000 = 24 hours)
SESSION_MAX_AGE=86400000

# ========================================
# Vertex AI Configuration (for AI features)
# ========================================
# Region for Vertex AI API (must match your GCP region)
# Recommended: us-central1, us-east4, us-west1, europe-west4, asia-southeast1
VERTEX_AI_LOCATION=us-central1

# Vertex AI Model Selection
# Options:
#   - gemini-1.5-pro: Best for detailed analysis (higher cost, slower)
#   - gemini-1.5-flash: Best for quick responses and OCR (lower cost, faster)
#   - gemini-1.0-pro: General purpose (medium cost/speed)
VERTEX_AI_MODEL=gemini-1.5-pro
# Model to use for resume analysis
VERTEX_AI_MODEL=gemini-1.5-pro

# ========================================
# Application Configuration
# ========================================
# Base URL for API calls (leave empty for same-origin)
VITE_API_BASE=

# Environment
NODE_ENV=development
